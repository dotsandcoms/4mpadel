import { Placeholder, Screen } from '@/components/screen';

export default function CalendarScreen() {
  return (
    <Screen eyebrow="What's on" title="Calendar">
      <Placeholder label="Upcoming tournaments and leagues, with entry and payment." />
    </Screen>
  );
}
