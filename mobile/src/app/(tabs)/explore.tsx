import { Placeholder, Screen } from '@/components/screen';

export default function ExploreScreen() {
  return (
    <Screen eyebrow="Where to play" title="Explore">
      <Placeholder label="Clubs, approved coaches, organisations and federations — consolidated into one browse surface." />
    </Screen>
  );
}
